import {token,logout} from './auth';export class ApiError extends Error{constructor(message:string,public status?:number){super(message)}}async function request(path:string,init:RequestInit={}){const headers={...(init.body?{'Content-Type':'application/json'}:{}),...(token.value?{Authorization:`Bearer ${token.value}`}:{}),...(init.headers||{})};const response=await fetch(path,{...init,headers});if(response.status===401){logout();throw new ApiError('A munkamenet lejárt.',401)}if(!response.ok){let body:any={};try{body=await response.json()}catch{}throw new ApiError(Array.isArray(body.details)&&body.details.length?body.details.join('\n'):body.message||'A kérés nem sikerült.',response.status)}return response}export async function api(path:string,init:RequestInit={}){const response=await request(path,init);return response.status===204?null:response.json()}export async function apiBlob(path:string,init:RequestInit={}){return (await request(path,init)).blob()}
